node_modules/
.env
.env.local
.env.production
.env.*.local
*.log
npm-debug.log*
.DS_Store

# Capacitor: generated web bundle (rebuilt by scripts/build-www.js)
www/

# Native projects are committed so Xcode/Android Studio can open them directly.
# Each platform has its own .gitignore for build artifacts / public web copies.
.tmp-sprites-api.json
debug.html
.vapid-keys.json
